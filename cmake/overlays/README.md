Soft present moved into **rlsw-cc** (`rlsw-cc/cmake/overlays/`).

`rlsw_cc_overlay_raylib()` applies it by default (`RLSW_CC_SOFT_PRESENT=ON`).
`cmake/SoftPresent.cmake` remains as thin wrappers for older call sites.
